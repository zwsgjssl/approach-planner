{
  "name": "approach-planner",
  "private": true,
  "description": "Approach Planner のブラウザテスト(Playwright)",
  "scripts": {
    "test": "node tests/regress.js && node tests/drawer.js && node tests/offline.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.55.0"
  }
}
